{
  "name": "dic",
  "version": "0.9.5",
  "description": "dependency injection container",
  "main": "dist/index.js",
  "scripts": {
    "flow": "flow",
    "test": "mocha --require babel-core/register --require babel-polyfill \"test/**/*.js\"",
    "build": "flow && babel src -d dist && flow-copy-source src dist"
  },
  "author": "pawel brzezinski",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.2",
    "flow-bin": "^0.63.1",
    "flow-copy-source": "^1.2.2",
    "mocha": "^5.0.0"
  }
}
